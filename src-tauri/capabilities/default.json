{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for LoreKeeper app",
  "windows": ["_all_"],
  "permissions": [
    "dialog:default",
    "fs:default",
    "store:default",
    "os:default"
  ]
}
